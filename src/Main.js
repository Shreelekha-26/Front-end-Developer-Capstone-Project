import React, { useReducer } from 'react';
import { useNavigate } from 'react-router-dom';
import BookingForm from './BookingForm';

const Main = () => {
  // Your code with submitForm function goes here
};

export default Main;