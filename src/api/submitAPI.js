// submitAPI.js

/**
 * Simulates submission of booking form data to an API.
 * 
 * @param {Object} formData - The data from the booking form (e.g., { name: 'John', date: '2025-08-01' }).
 * @returns {boolean} - Always returns true to simulate a successful submission.
 */
export function submitAPI(formData) {
  // Log the form data to console (for debugging or testing purposes)
  console.log('Submitting form data:', formData);

  // Simulate a successful API response
  return true;
}
